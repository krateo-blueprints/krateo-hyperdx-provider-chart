# Canonical OCI publish for a krateo chart repo using the charts/* layout. On a semver tag
# push, packages every chart under charts/* (building deps first) and pushes each to the
# consolidated registry oci://ghcr.io/braghettos/krateo. Chart versions are literal (pinned by
# the installer), so no version substitution — appVersion stays the real upstream app version.
name: Release charts to OCI

on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: read
  packages: write

env:
  OCI_REGISTRY: oci://ghcr.io/braghettos/krateo

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.19.0

      - name: Log in to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | \
            helm registry login ghcr.io --username "${{ github.actor }}" --password-stdin

      - name: Package and push charts
        run: |
          set -euo pipefail
          mkdir -p .packaged
          for dir in charts/*/; do
            name="$(basename "$dir")"
            echo "::group::Releasing chart ${name}"
            if grep -q '^dependencies:' "${dir}Chart.yaml"; then
              helm dependency update "${dir}"
            fi
            helm package "${dir}" --destination .packaged
            pkg="$(ls .packaged/${name}-*.tgz | head -n1)"
            echo "Pushing ${pkg} to ${OCI_REGISTRY}"
            helm push "${pkg}" "${OCI_REGISTRY}"
            echo "::endgroup::"
          done
